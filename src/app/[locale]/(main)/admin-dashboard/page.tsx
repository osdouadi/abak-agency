import { checkRole } from '@/lib/roles'
import { redirect } from 'next/navigation'
import React from 'react'

const page = () => {
 //// if(!checkRole("admin")) {
///redirect("/")
//  }

  return (
    <main>
      
    </main>
  )
}

export default page